<%= t('mailer_hello') %>

<% if @resource.try(:unconfirmed_email?) %>
<%= t('mailer_email_changed_unconfirmed', email: @resource.unconfirmed_email) %>
<% else %>
<%= t('mailer_email_changed_confirmed', email: @resource.email) %>
<% end %>

<%= t('mailer_email_changed_unauthorized') %>
<%# The layout will add the unique ID footer %>
